<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>temp_clean_arch.Shared</RootNamespace>
    <AssemblyName>temp_clean_arch.Shared</AssemblyName>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
